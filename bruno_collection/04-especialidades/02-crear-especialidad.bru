meta {
  name: 2. Crear Especialidad
  type: http
  seq: 2
}

post {
  url: http://localhost:5000/especialidades
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "nombre": "Cardiología",
    "descripcion": "Especialidad médica que se ocupa del corazón y el sistema circulatorio"
  }
}